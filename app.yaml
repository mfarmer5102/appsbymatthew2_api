#runtime: python37 # or python38 for Python 3.8
#entrypoint: python server.py

runtime: python38

#instance_class: F2

# env_variables:
#  PORT: $PORT
#  DB_NAME: $DB_NAME
#  DB_URL: $DB_URL

#handlers:
# Matches requests to /images/... to files in static/images/...
# - url: /images
#   static_dir: static/images

#- url: /.*
  # secure: always
  # redirect_http_response_code: 301
#  script: auto